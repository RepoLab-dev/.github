# RepoLabs org profile

Public GitHub organization profile lives in `profile/README.md`.
Brand assets live in `assets/` (SVG mark plus 256px PNG for Settings → General).

Upload `assets/logo-only-256.png` as the organization avatar.
